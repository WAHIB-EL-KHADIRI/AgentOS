# Python Examples

## Prerequisites

```bash
pip install httpx
```

## Running

```bash
python agent_example.py
```

Make sure the AgentOS runtime is running first:

```bash
cargo run -p agentos-cli -- run
```
